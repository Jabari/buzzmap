angular.module('AppCtrl', [])
.controller('SearchCtrl', ['$scope', function($scope) {
	ionic.Platfrom.ready(function(){

	});
// this module will contain logic for searching videos via tag, location, title, etc
	
	}]);